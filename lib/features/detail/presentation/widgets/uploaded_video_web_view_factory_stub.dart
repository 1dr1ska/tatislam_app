/// Non-web stub — on non-web platforms the uploaded video is played through a
/// WebViewController loading an inline HTML `<video>` element instead.
String? registerUploadedVideoView(String url) => null;